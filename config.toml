# Supabase Edge Functions Configuration
# See: https://supabase.com/docs/guides/functions/function-configuration

# =============================================================================
# Core Functions
# =============================================================================

[functions.resize-tinify-upload-image]
verify_jwt = false

[functions.geolocate-user]
verify_jwt = false

[functions.get-translations]
verify_jwt = false

# =============================================================================
# Bot Webhooks
# =============================================================================

[functions.telegram-bot-foodshare]
verify_jwt = false

[functions.whatsapp-bot-foodshare]
verify_jwt = false

# =============================================================================
# Subscription/Payment Webhooks
# =============================================================================

[functions.subscription-webhook]
verify_jwt = false

[functions.subscription-cron]
verify_jwt = false

# =============================================================================
# Notification System
# =============================================================================

[functions.notify-new-post]
verify_jwt = false

# =============================================================================
# UNIFIED NOTIFICATION API v1 (MASTER)
# =============================================================================
# Enterprise-grade unified notification API consolidating ALL channels:
# - Channels: Email (Resend/Brevo/SES/MailerSend), Push (FCM/APNs/WebPush), SMS, In-App
# - Send: POST /send, /send/batch, /send/template
# - Digest: POST /digest/process (cron)
# - Preferences: GET/PUT /preferences, POST/DELETE /preferences/dnd
# - Webhooks: POST /webhook/:provider (all email + push providers)
# - Dashboard: GET /health, /stats, /dashboard
# - Admin: /admin/* (queue, suppression, channel control)
# Auth handled internally per-route

[functions.api-v1-notifications]
verify_jwt = false
